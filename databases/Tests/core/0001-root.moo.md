---
id: "#1"
name: $root
owner: "#0"
parent:
location:
flags:
  - readable
  - fertile
aliases: []
updated: 2026-06-02T18:52:55-05:00
---

# $root

```yaml
name: description
type: string
value: "You see nothing special."
flags:
  - readable
```

```yaml
name: f
type: integer
value: 1
flags:
  - readable
```

```yaml
name: r
type: integer
value: 1
flags:
  - readable
```

```yaml
name: w
type: integer
value: 0
flags:
  - readable
```

## Verb: moveto

```yaml
names: ["moveto"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return `move(this, args[1]) ! ANY => E_NONE';
```

## Verb: title

```yaml
names: ["title"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return this.name;
```

## Verb: titlec

```yaml
names: ["titlec"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return `this.namec ! E_PROPNF => this:title()';
```

## Verb: tell

```yaml
names: ["tell"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
this:notify(tostr(@args));
```

## Verb: notify

```yaml
names: ["notify"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
if (is_player(this))
  notify(this, @args);
endif
```

## Verb: look_self

```yaml
names: ["look_self"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
desc = this:description();
if (desc)
  player:tell(desc);
else
  player:tell("You see nothing special.");
endif
```

## Verb: match

```yaml
names: ["match"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
c = this:contents();
return $string_utils:match(args[1], c, "name", c, "aliases");
```

## Verb: contents

```yaml
names: ["contents"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return this.contents;
```

## Verb: description

```yaml
names: ["description"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return this.description;
```

## Verb: set_description

```yaml
names: ["set_description"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
if (typeof(desc = args[1]) in {LIST, STR})
  this.description = desc;
  return 1;
else
  return E_TYPE;
endif
```

## Verb: huh

```yaml
names: ["huh"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
$command_utils:do_huh(verb, args);
```

## Verb: set_message

```yaml
names: ["set_message"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
return `this.(args[1] + "_msg") = args[2] ! ANY' && 1;
```

## Verb: get_message

```yaml
names: ["get_message"]
dobj: this
prep: none
iobj: this
owner: "#0"
flags:
  - readable
  - executable
```

```csharp
if ((t = typeof(msg = `this.(args[1] + "_msg") ! ANY')) in {ERR, STR} || (t == LIST && msg && typeof(msg[1]) == STR))
  return msg;
else
  return {1, msg};
endif
```

